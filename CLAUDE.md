# CLAUDE.md — SALESCORE PPT生成 プロジェクト指示書

このリポジトリでは `generate_ppt.py` を使って Markdown → PowerPoint (.pptx) を生成する。
**デザイン仕様は `salescore-pptx-format` スキルをベースとし、最優先とする。**

---

## 使い方

```bash
python generate_ppt.py input.md output.pptx [logo.png]
```

依存: `python-pptx`, `lxml`（`pip install -r requirements.txt`）

---

## Markdown 記法

| 記法 | 生成されるスライド |
|------|-----------------|
| `# タイトル` | タイトルスライド（H1直後の非見出し行がサブタイトル） |
| `### 節名` または `### 1. 節名 \| タイトル` | セクション区切りスライド（全面ダークネイビー） |
| `## タイトル` または `## 1.1. 節番号 \| タイトル` | コンテンツスライド |
| `> リード文` | タイトル直下の概要メッセージ（18pt・全幅） |
| `- 箇条書き` | 標準箇条書き（● Accent） |
| `  - サブ項目` | 字下げ箇条書き（– Muted、インデント2スペース以上） |
| `1. 番号付き` | 番号付きリスト（番号 Accent Bold） |
| `---` | コンテンツ内の視覚的区切り線 |
| `\| ヘッダー \| … \|` + `\|---\|` | Markdownテーブル → pptxテーブル |
| `::left [タイトル]` / `::right [タイトル]` / `:::` | 2カラムレイアウト |

---

## デザイン原則（salescore-pptx-format ベース）

**スタイル**: SALESCOREの公式フォーマットに完全準拠
- フォントは **Meiryo UI** を例外なく全要素に使用
- 余白を十分に取る（窮屈にしない）
- 装飾より構造・読みやすさを優先
- 縦の黒い長方形（装飾ライン）は使用しない

---

## カラーパレット（変更不可）

| 変数名 | HEX | 用途 |
|--------|-----|------|
| `COLOR_PRIMARY` | `#1A1A1A` | 基本テキスト・ヘッダーライン・フッターライン |
| `COLOR_ACCENT` | `#0066CD` | 箇条書き記号・番号・強調ボックス |
| `COLOR_DANGER` | `#E74C3B` | 重要ポイント（ごくわずか） |
| `COLOR_MUTED` | `#888888` | フッターテキスト・スライド番号 |
| `COLOR_WHITE` | `#FFFFFF` | 背景・テーブルデータ行 |
| `COLOR_LIGHT_GRAY` | `#F5F5F5` | 薄いグレー背景 |
| `COLOR_MID_GRAY` | `#CCCCCC` | 区切り線・縦区切り |
| `COLOR_STRIPE` | `#F5F7FA` | テーブルストライプ（偶数行） |
| `COLOR_DARK_NAVY` | `#1D3461` | タイトルスライド背景・セクション全面 |
| `COLOR_NAVY` | `#1B4FA8` | セクションスライドアクセント |
| `COLOR_BLUE` | `#1E6FD8` | セクションアクセントライン |

---

## スライドレイアウト仕様

### スライドサイズ
- 16:9 / 幅 13.33インチ × 高さ 7.5インチ

### 共通要素（全スライド）— salescore-pptx-format 実測値

| 要素 | 位置・サイズ | スタイル |
|------|------------|---------|
| **ロゴ** | x=10.213", y=0.091", w=2.205", h=0.512" | logo.png（なければテキスト代替） |
| **ヘッダーライン** | x=0.917", y=0.689", w=11.5", h=0 | `#1A1A1A` 1.5pt solid |
| **フッターライン** | x=0.917", y=6.951", w=11.5", h=0 | `#1A1A1A` 1.5pt solid |
| **フッターテキスト** | x=0.917", y=7.0", w=5.5", h=0.25" | 9pt Meiryo UI `#888888` 左揃え |
| **ページ番号** | x=11.5", y=7.0", w=1.5", h=0.25" | 12pt Meiryo UI `#888888` 右揃え |

### タイトルスライド（補足レイアウト）
- 上部58%: `COLOR_DARK_NAVY`
- 下部42%: `COLOR_WHITE`
- 境界: `COLOR_NAVY` 6ptライン
- タイトル: 34pt Bold White Meiryo UI
- サブタイトル: 14pt `#CCD8F0` Meiryo UI

### セクション区切りスライド（補足レイアウト）
- 全面: `COLOR_DARK_NAVY`
- 節番号（任意）: 11pt Bold `COLOR_BLUE` Meiryo UI
- タイトル: 36pt Bold White Meiryo UI
- 下部アクセントライン: 幅2.5インチ・3pt `COLOR_BLUE`

### コンテンツスライド — salescore-pptx-format 実測値

```
y=0.276"  タイトル（h=0.315", 21pt Bold Meiryo UI）
y=0.689"  ══ ヘッダーライン ══（1.5pt #1A1A1A）
y=0.700"  リードテキスト（h=0.688", 18pt Meiryo UI）
y=1.4"    コンテンツエリア開始
y=6.951"  ══ フッターライン ══
y=7.0"    フッターテキスト / ページ番号
```

- 節番号（任意）: 9pt Bold `#888888` Meiryo UI（タイトル上）
- タイトル: x=0.906", y=0.276", w=9.5", h=0.315", **21pt Bold** `#1A1A1A`
- コンテンツ左端: x=0.917"
- コンテンツ幅: 11.5"（右端 12.417"）

---

## コンテンツ描画ルール

### フォントサイズ階層（salescore-pptx-format 準拠）

| 用途 | サイズ | 備考 |
|------|--------|------|
| スライドタイトル | **21pt Bold** | 固定 |
| リードテキスト | **18pt** | 固定 |
| ボディ（標準） | **16pt** | 箇条書き・番号付きメイン |
| ボディ（補足） | **14pt** | サブ箇条書き・テーブル |
| フッター | **9pt** | 固定 |
| ページ番号 | **12pt** | 固定 |

**11pt以下はフッター専用。それ以外では使用しない。**

### テキスト（箇条書き・番号付き）
- 箇条書き: `●` 16pt Accent + テキスト 16pt `COLOR_PRIMARY`、上余白6pt
- サブ箇条書き: `–` 14pt Muted + テキスト 14pt `COLOR_PRIMARY`、上余白3pt
- 番号付き: 番号 16pt Bold Accent + テキスト 16pt `COLOR_PRIMARY`
- 区切り線（`---`）: `COLOR_MID_GRAY` 1pt 横線

### テーブル
- ヘッダー行高さ: 0.40インチ、背景 `COLOR_DARK_NAVY`、テキスト 14pt Bold White 中央揃え
- データ行高さ: 0.35インチ、ストライプ（偶数行 `COLOR_STRIPE` / 奇数行 White）
- テキスト: 14pt `COLOR_PRIMARY` 左揃え
- 列幅: 均等分割

### 2カラムレイアウト
- カラム間ギャップ: 0.35インチ
- 中央縦区切り線: `COLOR_MID_GRAY` 1pt
- カラムタイトル（任意）: 14pt Bold `COLOR_DARK_NAVY`、高さ0.38インチ

### セグメントベースの高さ計算
- テーブルは固定高さ（行数×行高さ）を先に確保
- 残り高さをテキスト/カラムセグメントで等分
- リード文エリア: 0.688インチ固定

---

## 実装上の重要ルール

1. **フォント**: 全要素に `run.font.name = "Meiryo UI"` を必ず設定
2. **シャドウ除去**: すべてのシェイプに `remove_shadow()` を適用（テーマ継承を上書き）
3. **テーブルセル背景**: `set_cell_bg()` でXML直接操作（python-pptxのAPIでは不完全）
4. **テキストボックス**: `word_wrap = True` を必ず設定
5. **スライドレイアウト**: 常に `prs.slide_layouts[6]`（blank）を使用
6. **縦の装飾ライン**: 使用しない

---

## ファイル構成

```
generate_ppt.py   # メインスクリプト（上記仕様を維持）
test_sample.md    # テスト用サンプルMarkdown
demo_sample.md    # 見本資料Markdown
logo.png          # SALESCOREロゴ（右上に配置）
requirements.txt  # python-pptx, lxml
SETUP.md          # Google Apps Script リマインダー設定手順
reminder.gs       # Google Apps Script コード
```

---

## 新機能追加時のチェックリスト

- [ ] フォントがすべて **Meiryo UI** になっているか
- [ ] フォントサイズの階層（21/18/16/14/12/9pt）を崩していないか
- [ ] 全スライドにロゴ・ヘッダーライン・フッターライン・ページ番号が入っているか
- [ ] ヘッダーライン座標: x=0.917", y=0.689", w=11.5"
- [ ] フッターライン座標: x=0.917", y=6.951", w=11.5"
- [ ] テーブルセル背景は `set_cell_bg()` で設定しているか
- [ ] シャドウ除去 `remove_shadow()` を適用しているか
- [ ] 縦の装飾ラインを追加していないか
