# CLAUDE.md — PPTスキルビルディング プロジェクト指示書

このリポジトリでは `generate_ppt.py` を使って Markdown → PowerPoint (.pptx) を生成する。
以下の設計原則・仕様を**必ず遵守**すること。品質を下げる変更・省略は不可。

---

## 使い方

```bash
python generate_ppt.py input.md output.pptx [logo.png]
```

依存: `python-pptx`, `lxml`（`pip install -r requirements.txt`）

---

## Markdown 記法

| 記法 | 生成されるスライド |
|------|-----------------|
| `# タイトル` | タイトルスライド（H1直後の非見出し行がサブタイトル） |
| `### 節名` または `### 1. 節名 \| タイトル` | セクション区切りスライド（全面ダークネイビー） |
| `## タイトル` または `## 1.1. 節番号 \| タイトル` | コンテンツスライド |
| `> リード文` | タイトル直下の概要メッセージ（小フォント・全幅） |
| `- 箇条書き` | 標準箇条書き（● Navy） |
| `  - サブ項目` | 字下げ箇条書き（– Gray、インデント2スペース以上） |
| `1. 番号付き` | 番号付きリスト（番号 Navy Bold） |
| `---` | コンテンツ内の視覚的区切り線 |
| `\| ヘッダー \| … \|` + `\|---\|` | Markdownテーブル → pptxテーブル |
| `::left [タイトル]` / `::right [タイトル]` / `:::` | 2カラムレイアウト |

---

## デザイン原則

**スタイル**: コンサルティングファーム・外資金融スタイル（McKinsey / Goldman Sachs 風）
- 余白を十分に取る（窮屈にしない）
- 文字サイズの階層を明確に保つ
- 装飾より構造・読みやすさを優先

---

## カラーパレット（絶対に変更しない）

| 変数名 | HEX | 用途 |
|--------|-----|------|
| `COLOR_DARK_NAVY` | `#1D3461` | タイトルスライド背景・セクション全面 |
| `COLOR_NAVY` | `#1B4FA8` | 箇条書き記号・番号・アクセント |
| `COLOR_BLUE` | `#1E6FD8` | セクションアクセントライン |
| `COLOR_TEXT` | `#1A1A1A` | 本文テキスト |
| `COLOR_BLACK` | `#000000` | ヘッダーライン・フッターライン |
| `COLOR_SECTION_NUM` | `#55657A` | 節番号グレー |
| `COLOR_GRAY` | `#808080` | フッターテキスト・サブ記号 |
| `COLOR_GRAY_LIGHT` | `#CCCCCC` | 区切り線・縦区切り |
| `COLOR_STRIPE` | `#F5F7FA` | テーブルストライプ（偶数行） |
| `COLOR_WHITE` | `#FFFFFF` | 背景・テーブルデータ行 |

---

## スライドレイアウト仕様

### スライドサイズ
- 16:9 / 幅 13.33インチ × 高さ 7.5インチ

### 共通要素（全スライド）
- **ロゴ**: 右上・幅2.2インチ・余白0.15インチ（logo.pngが存在する場合）
- **フッターライン**: 黒・1.2pt・下部
- **フッターテキスト**: `Confidential All Rights Reserved SALESCORE Inc.`（8pt Gray）
- **ページ番号**: 右下（9pt Gray）

### タイトルスライド
- 上部58%: `COLOR_DARK_NAVY`
- 下部42%: `COLOR_WHITE`
- 境界: `COLOR_NAVY` 6ptライン
- タイトル: 34pt Bold White
- サブタイトル: 13pt `#CCD8F0`（薄ブルーホワイト）

### セクション区切りスライド
- 全面: `COLOR_DARK_NAVY`
- 節番号（任意）: 11pt Bold `COLOR_BLUE`
- タイトル: 36pt Bold White
- 下部アクセントライン: 幅2.5インチ・3pt `COLOR_BLUE`

### コンテンツスライド
- 背景: 純白
- 節番号（任意）: 9pt Bold `COLOR_SECTION_NUM`（タイトル上）
- タイトル: 26pt Bold `COLOR_TEXT`
- ヘッダーライン: 黒 1.8pt
- コンテンツ左余白: 0.50インチ
- 右余白: 0.35インチ

---

## コンテンツ描画ルール

### テキスト（箇条書き・番号付き）
- 箇条書き: `●` 12pt Navy + テキスト 13pt `COLOR_TEXT`、上余白6pt
- サブ箇条書き: `–` 10pt Gray + テキスト 11pt `COLOR_TEXT`、上余白3pt
- 番号付き: 番号 13pt Bold Navy + テキスト 13pt `COLOR_TEXT`
- 区切り線（`---`）: `COLOR_GRAY_LIGHT` 1pt 横線

### テーブル
- ヘッダー行高さ: 0.40インチ、背景 `COLOR_DARK_NAVY`、テキスト 11pt Bold White 中央揃え
- データ行高さ: 0.35インチ、ストライプ（偶数行 `COLOR_STRIPE` / 奇数行 White）
- テキスト: 11pt `COLOR_TEXT` 左揃え
- 列幅: 均等分割

### 2カラムレイアウト
- カラム間ギャップ: 0.35インチ
- 中央縦区切り線: `COLOR_GRAY_LIGHT` 1pt
- カラムタイトル（任意）: 13pt Bold `COLOR_DARK_NAVY`、高さ0.38インチ

### セグメントベースの高さ計算
- テーブルは固定高さ（行数×行高さ）を先に確保
- 残り高さをテキスト/カラムセグメントで等分
- リード文エリア: 0.65インチ固定

---

## 実装上の重要ルール

1. **シャドウ除去**: すべてのシェイプに `remove_shadow()` を適用（テーマ継承を上書き）
2. **テーブルセル背景**: `set_cell_bg()` でXML直接操作（python-pptxのAPIでは不完全）
3. **テキストボックス**: `word_wrap = True` を必ず設定
4. **スライドレイアウト**: 常に `prs.slide_layouts[6]`（blank）を使用
5. **フォント**: システムフォントに依存しない設定（サイズ・色を明示指定）

---

## ファイル構成

```
generate_ppt.py   # メインスクリプト（編集時は上記仕様を維持）
test_sample.md    # テスト用サンプルMarkdown
logo.png          # ロゴ画像（右上に配置）
requirements.txt  # python-pptx, lxml
SETUP.md          # Google Apps Script リマインダー設定手順
reminder.gs       # Google Apps Script コード
```

---

## 新機能追加時のチェックリスト

- [ ] カラーパレットは既存変数を使用しているか
- [ ] フォントサイズの階層（34/26/13/11/9/8pt）を崩していないか
- [ ] 余白（左0.50インチ・右0.35インチ）を守っているか
- [ ] 全スライドにロゴ・フッター・ページ番号が入っているか
- [ ] テーブルセル背景は `set_cell_bg()` で設定しているか
- [ ] シャドウ除去 `remove_shadow()` を適用しているか
